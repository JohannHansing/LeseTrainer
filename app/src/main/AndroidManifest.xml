<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lese.trainer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeseTrainer">
        <activity
            android:name=".WordSelectActivity"
            android:exported="false" />
        <activity
            android:name=".SelectWordsFromListActivity"
            android:parentActivityName=".ViewWordsListActivity" />
        <activity
            android:name=".ViewWordsListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".GetWordsActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>